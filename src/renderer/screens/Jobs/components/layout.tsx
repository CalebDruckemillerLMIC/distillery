import React from 'react'
import { Container } from 'reactstrap'

type Props = {
    
}

function Layout({}: Props) {
  return (
    <Container className='bg=light border'>Layout</Container>
  )
}

export default Layout